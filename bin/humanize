#!/usr/bin/env ruby

# Purpose: 
#   Convert a number of bytes in a more "human readable" representation
#
# Usage:
#   you@box$ humanize 1234567  
#   1.18 MB

require "action_view"
include ActionView::Helpers::NumberHelper

bytes = Integer(ARGV.shift)

puts ActionView::Helpers::NumberHelper.number_to_human_size(bytes) 
